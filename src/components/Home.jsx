// import { useContext } from "react";
// import { AuthContext } from "../Provider/AuthProvider";


const Home = () => {
  //  const authInfo = useContext(AuthContext);
  //  console.log(authInfo);
  return (

    <div>

     <h2 className="text-3xl text-center">This is <span className="text-green-500 font-semibold">Abrar's</span> Home  </h2>

    </div>

  );
};

export default Home;
